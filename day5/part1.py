# each map is a range of values from source_start to source_start + range
# and an offset = dest_start - source_start 

# for each seed
#   for each map
#       if value in one of the ranges;
#           value += offset for that range